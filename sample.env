# Server settings
HOST=0.0.0.0
PORT=8088

# Required: Client access token
# All requests must include Authorization: Bearer <UNIFIED_TOKEN>
# The real provider API keys stay on the server and are never exposed to clients.
UNIFIED_TOKEN=change-me-to-a-strong-random-string
