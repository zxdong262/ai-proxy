# Server settings
HOST=0.0.0.0
PORT=8088

# Optional: Public deployment security
# When UNIFIED_TOKEN is set, all routes require this token in Authorization: Bearer header.
# UNIFIED_TOKEN=your-unified-access-token
