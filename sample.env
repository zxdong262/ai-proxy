# Server settings
HOST=0.0.0.0
PORT=8088

# Optional: Public deployment security
# When both SECRET and UNIFIED_TOKEN are set, all config routes require a valid JWT.
# The JWT must be signed with SECRET and contain { "token": UNIFIED_TOKEN } in its payload.
# SECRET=your-jwt-signing-secret
# UNIFIED_TOKEN=your-unified-access-token
